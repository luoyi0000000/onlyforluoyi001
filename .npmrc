# pnpm keeps its content-addressable store on the same volume as the project so
# packages can be hard linked instead of copied. The project lives on F:, so the
# store lives on F: too. Nothing is ever written to C:.
store-dir=F:/.pnpm-store

# Reproducible installs: pinned versions only, no silent range widening.
save-exact=true
